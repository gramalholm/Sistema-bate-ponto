
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Funcionario {
  id           String      @id @default(auto()) @map("_id") @db.ObjectId
  senha        String
  name         String
  cargo        String
  turno        String
  email        String
  Hora_chegada String?
  Hora_saida   String?
  Horas_totais   String
  registros    Registro[]
}

model Ponto {
  codigo       String      @id @default(auto()) @map("_id") @db.ObjectId
  tipo         String
  horario      String
  registros    Registro[]
}

model Registro {
  id             String      @id @default(auto()) @map("_id") @db.ObjectId
  funcionario    Funcionario @relation(fields: [funcionarioId], references: [id])
  ponto          Ponto       @relation(fields: [pontoCod], references: [codigo])
  funcionarioId  String      @db.ObjectId
  pontoCod       String      @db.ObjectId
  data           String
}
